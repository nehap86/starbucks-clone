version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install
      - npm run build
run:
  command: npx serve -s dist -l tcp://0.0.0.0:8080
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production
